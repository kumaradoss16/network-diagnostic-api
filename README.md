# network-diagnostic-api
Network Diagnostic API is a Python/FastAPI backend that provides REST endpoints for network troubleshooting, including ping, DNS lookup, TCP port checks, HTTP/HTTPS connectivity, latency measurement, and combined diagnostics. Built with async programming, validation, structured logging, and centralized error handling.
